# XNM Airdrop Configuration

# Token mint address for XNM token (REQUIRED)
TOKEN_MINT=your_token_mint_address_here

# Solana RPC endpoint
RPC_ENDPOINT=https://rpc.testnet.x1.xyz

# Token decimals (6 for X1 network)
DECIMALS=6

# Set to true for testing, false for actual airdrop
DRY_RUN=true

# Log files for tracking state
LOG_FILE=./airdrop-log.json
FAIL_FILE=./airdrop-failures.json

# Path to keypair file for the payer wallet
KEYPAIR_PATH=~/.config/solana/your-keypair.json

# API endpoint for fetching miner data
API_ENDPOINT=https://xenblocks.io/v1/leaderboard?limit=10000&require_sol_address=true
